export type Emaildata = {
  bookingId: string;
  paymentId?: string;
  userId: string;
  auth0Id: string;
  tenantId: string;
};

export type InvoiceData = {
  bookingId: string;
  paymentId?: string;
  userId: string;
  auth0Id: string;
  tenantId: string;
};